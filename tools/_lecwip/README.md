# 개념 → 강의 배선, 집필 중인 조각

`tools/lec_link.py --absorb` 가 여기 있는 `*.json` 을 읽어
`concept-lecture-dt.json` 의 표로 옮긴다.

**임시 폴더가 아니라 저장소 안에 둔 이유**가 있다. 처음에는 `/tmp` 아래
작업 폴더에 두었는데, 컨테이너가 사라지면서 여덟 시간치 배선 작업(에이전트
36명 · 오개념 792가지)이 통째로 없어졌다(2026-09-02). 되살릴 방법이 없었다 —
표에 아직 안 옮긴 조각이었기 때문이다.

저장소 안에 두면 자동 저장이 4분마다 밀어 넣는다.

⚠ 여기 있는 것은 **작업 중인 원고**다. 검증을 다 거치지 않은 조각이 섞여
있을 수 있으니, 화면이 쓰는 것은 `--absorb` 로 표에 옮긴 것뿐이다.
